git clone https://github.com/mma44331/tests-week-11.git

minikube start

git branch dockerfile

git branch service

git branch commands

git branch pod

git branch screenshot

git checkout commands

docker build -t my-app .       #בניית ה image

docker run  -p 8080:8080 my-app  #זה בדיקת הרצה של ה image